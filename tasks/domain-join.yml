---
- include_vars: "files/domain-join/kevtx-lan/main.yml"
- include_vars: "files/domain-join/kevtx-lan/vault.yml"

- include_vars: "files/domain-join/dependencies.yml"

- name: Install dependencies
  become: true
  apt: 
    name: "{{ item }}"
    state: latest
    cache_valid_time: 604800
  with_items: "{{ dependencies }}"

- name: Install pexpect via pip
  become: true
  pip:
    name: pexpect
    state: present

- name: Copy realmd.conf
  become: true
  template: 
    src: files/domain-join/realmd.conf.j2
    dest: /etc/realmd.conf
    owner: root
    group: root
    mode: 0644

- name: Discover realm
  command: /bin/bash -c "/usr/sbin/realm discover {{ ad.domain }}"
  register: realm_discover_results

- name: Discover realm debug
  debug: 
    msg: "{{ realm_discover_results.stdout }}"

- name: Checking to see if system is already joined to AD
  command: /bin/bash -c "/usr/sbin/realm list"
  register: realm_list_results

- name: Debug realm_list_results
  debug:
    msg: "{{ realm_list_results.stdout }}"

- name: Join system to AD
  become: true
  expect:
    command: /bin/bash -c "/usr/sbin/realm join {{ ad.domain }} --computer-ou='OU=Computers,DC=kevtx,DC=lan' --user={{ ad.user }}"
    responses:
      (?i)Password: "{{ pw__domainjoin }}"
  ignore_errors: true
  when: realm_list_results.stdout == ""

- name: Copy sssd.conf
  template:
    src: files/domain-join/sssd.conf.j2
    dest: /etc/sssd/sssd.conf
    owner: root
    group: root
    mode: 0600

- name: Restart 'sssd' systemd service
  systemd:
    daemon_reload: yes
    enabled: yes
    state: restarted
    name: sssd.service

- name: Copy pam common-session
  template: 
    src: files/domain-join/common-session.j2
    dest: /etc/pam.d/common-session
    owner: root
    group: root
    mode: 0644

- name: Copy sshd_config.d files
  copy:
    src: "files/domain-join/sshd_config/{{ item }}.conf"
    dest: "/etc/ssh/sshd_config.d/{{ ad.domain }}--{{ item }}.conf"
    owner: root
    group: root
    mode: ug=r,o-rwx
  with_items:
    - enable-pam
    - grp-allow_key
    - grp-allow_pwd

- name: Restart 'sshd' systemd service
  systemd:
    daemon_reload: yes
    enabled: yes
    state: reloaded
    name: sshd.service

#- name: Add domain admins group to sudoers
  #lineinfile:
    #dest: "/etc/sudoers.d/{{ ad.domain | replace('.','_') }}"
    #line: '%domain\ admins  ALL=(ALL:ALL)   ALL'
    #regexp: '^%domain'
    #state: present
    #owner: root
    #group: root
    #mode: u=r,go-rwx
    #create: yes
